spring:
#  config:
#    activate:
#      on-profile: dev
  datasource:
    url: jdbc:mysql://localhost:3306/ctrl_zero
    username: root
    password: 12345678

  devtools:
    livereload:
      enabled: true
    remote:
      restart:
        enabled: true