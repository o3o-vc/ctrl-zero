spring:
  profiles:
    active: dev
  application:
    name: ctrlzero
server:
  port: 8333
mybatis-flex:
  mapper-locations:
    - classpath*:/mapper/*.xml

---
spring:
  config:
    active:
      on-profile: dev
  datasource:
    url: jdbc:mysql://localhost:3306/ctrl_zero
    username: root
    password: 12345678